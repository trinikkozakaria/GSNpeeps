# Task 1 — Foundation, Contract, and Executable Skeletons

## Contents

[Metadata](#metadata) · [Tujuan](#tujuan) · [In scope](#in-scope) ·
[Out of scope](#out-of-scope) · [Keputusan sebelum implementasi](#keputusan-sebelum-implementasi) ·
[Milestone F0.1 — Repository and environment](#milestone-f01--repository-and-environment) ·
[Milestone F0.2 — Infrastructure topology](#milestone-f02--infrastructure-topology) ·
[Milestone A.1 — OpenAPI contract](#milestone-a1--openapi-contract) ·
[Milestone B-BE.1 — Backend skeleton](#milestone-b-be1--backend-skeleton) ·
[Milestone B-FE.1 — Frontend skeleton](#milestone-b-fe1--frontend-skeleton) ·
[File targets](#file-targets) · [Verification](#verification) ·
[Acceptance criteria](#acceptance-criteria) · [Exit gate](#exit-gate)

## Metadata

| Item | Value |
|---|---|
| Owner | Foundation/Backend for root and contract; Backend and Frontend for skeleton masing-masing |
| Suggested branches | `chore/infra-foundation`, `chore/openapi-gsnpeeps`, `feat/be-skeleton`, `feat/fe-skeleton` |
| Prompt | `A1-openapi.md`, `B-BE1-skeleton.md`, `B-FE1-skeleton.md` |
| Depends on | Six approved source documents and `CLAUDE.md` |
| Unblocks | All subsequent tasks |

Use separate focused branches/PRs when the repository workflow supports it. A branch name in
this document is proposed, not automatic authorization to create/push it.

## Tujuan

Produce a reproducible monorepo in which:

- Nginx serves/proxies the application.
- React builds static production assets.
- Go API and worker share code but run independently.
- PostgreSQL 16, Redis 7, and Nextcloud are internal services.
- OpenAPI is the contract between backend and frontend.
- Configuration, logging, errors, health, shutdown, tests, and migrations have stable
  foundations.

## In scope

- Root repository structure and ignore/editor baseline.
- Docker Compose and internal networking.
- Environment examples without real secrets.
- Backend module, API/worker composition roots, configuration, logging, response, database,
  Redis, router, middleware skeleton, migration runner, and health.
- Frontend React/Tailwind skeleton, design tokens, app shell, routing/provider boundary,
  feedback primitives, API/config boundary, test setup, and mock boundary.
- Complete OpenAPI 3.1 for the approved 46 operations.
- Reproducible commands for dev/build/format/lint/test/E2E/migration/worker.
- Basic CI-ready quality gates.

## Out of scope

- Business CRUD implementation beyond a health endpoint.
- Real employee, attendance, approval, and notification records.
- Production credentials, production deployment, DNS, or live TLS issuance.
- Libraries outside the approved baseline unless a task-specific extension is accepted.
- Refresh-token endpoint; current-user and password operations follow contract 0.4.0.
- Hiring Progress, Recruitment Cost, and Benefit beyond later placeholders.

## Keputusan stack final

Gunakan baseline berikut; jangan membuat proposal stack ulang.

### Backend

- Go, `net/http` + `gorilla/mux`.
- pgx/pgxpool, Goose, go-playground/validator.
- golang-jwt/jwt/v5, go-redis/v9, `log/slog`, Testify.
- Scheduler, password hashing, export, dan linter tambahan tetap task-specific.

### Frontend

- React JavaScript/JSX, Vite, React Router, Tailwind CSS.
- Axios, TanStack Query, React Hook Form, Zod.
- Vitest, Testing Library, Playwright, pnpm.
- Global client state, table/chart/icon, network mock, workbench, formatter, dan linter
  tambahan tetap task-specific.

## Milestone F0.1 — Repository and environment

- [ ] **F0.1.1** Create/verify target directories: `backend`, `frontend`, `docs`, `.claude`.
- [ ] **F0.1.2** Configure `.gitignore` for secrets, dependencies, build outputs, editor files,
  coverage, logs, local databases, and generated temporary artifacts.
- [ ] **F0.1.3** Create root and service `.env.example` files containing names and safe
  development placeholders only.
- [ ] **F0.1.4** Validate public frontend environment contains no secret.
- [ ] **F0.1.5** Define repository commands with pnpm for frontend and Go tooling for backend.
- [ ] **F0.1.6** Pin runtime/image versions according to approved support policy.
- [ ] **F0.1.7** Document local startup, migration, seed, test, and shutdown flow in the
  approved root documentation location.

Required environment groups:

- App environment, port/base URL, CORS.
- PostgreSQL connection.
- Redis connection and rate limits.
- JWT secret and eight-hour TTL.
- Nextcloud WebDAV URL/user/app password.
- Logging.
- Frontend app name/API base URL with bundler-specific public prefix only after approval.

## Milestone F0.2 — Infrastructure topology

- [ ] **F0.2.1** Define Compose services: `nginx`, `frontend`, `backend-api`, `cron-worker`,
  `postgres`, `redis`, `nextcloud`.
- [ ] **F0.2.2** Expose only Nginx publicly; keep other services on internal networks.
- [ ] **F0.2.3** Add persistent volumes for PostgreSQL, Redis if required, and Nextcloud data.
- [ ] **F0.2.4** Add service healthchecks/readiness dependencies without hiding startup
  failures.
- [ ] **F0.2.5** Run backend API and worker from the same codebase/image with different
  commands.
- [ ] **F0.2.6** Configure Nginx static assets, API proxy, request limits, and client-route
  fallback compatible with the selected frontend router.
- [ ] **F0.2.7** Ensure browser cannot access PostgreSQL, Redis, or Nextcloud credentials.
- [ ] **F0.2.8** Validate Compose configuration and document development port choices.

Do not treat Compose `depends_on` as proof that a dependency is ready.

## Milestone A.1 — OpenAPI contract

- [x] **A.1.1** Create `docs/openapi.yaml` as OpenAPI 3.1 with product metadata and `/api/v1`.
- [x] **A.1.2** Define Bearer authentication; leave `GET /health`, login, and approved
  self-reset public.
- [x] **A.1.3** Define standard success, list pagination, error, and validation-field schemas.
- [x] **A.1.4** Define reusable parameters for UUID, `page`, `limit`, approved filters, and
  export format.
- [x] **A.1.5** Document all 46 operations from contract revision 0.4.0.
- [x] **A.1.6** Define JSON and multipart request/response schemas with `snake_case`.
- [x] **A.1.7** Document role/scope requirements and relevant error codes per operation.
- [x] **A.1.8** Model file upload maximum 5 MB and approved MIME/extensions.
- [x] **A.1.9** Implement only approved auth operations; do not invent refresh,
  registration, or forgot-password email/OTP flows.
- [!] **A.1.10** Local YAML/operation/ref/response validation passed for 46 operations;
  final Redocly lint is pending because third-party package execution was blocked by the
  environment security policy.
- [x] **A.1.11** Make frontend schemas/mocks derive from this contract.

Any discrepancy between API Contract PDF and implementation must be fixed at the contract or
raised explicitly; do not hide it in a client mapper.

## Milestone B-BE.1 — Backend skeleton

- [ ] **B-BE.1.1** Initialize the approved Go module and reproducible dependencies.
- [ ] **B-BE.1.2** Create `cmd/api` and `cmd/worker` composition roots.
- [ ] **B-BE.1.3** Create `internal/config` with typed environment parsing and startup
  validation.
- [ ] **B-BE.1.4** Create structured logging with request/job correlation and secret/PII
  redaction.
- [ ] **B-BE.1.5** Create PostgreSQL connection/pool and context-aware health probe.
- [ ] **B-BE.1.6** Create Redis connection/pool and context-aware health probe.
- [ ] **B-BE.1.7** Define Nextcloud `FileStore` boundary without exposing credentials.
- [ ] **B-BE.1.8** Create standard response writer and typed error mapping.
- [ ] **B-BE.1.9** Create router and middleware order: request ID, recovery, access log,
  security/CORS, rate-limit boundary, auth/RBAC placeholders.
- [ ] **B-BE.1.10** Configure HTTP read/write/idle/header timeouts and body limits.
- [ ] **B-BE.1.11** Implement graceful startup/shutdown for API and worker.
- [ ] **B-BE.1.12** Implement migration command/runner; never auto-migrate schema in
  production startup.
- [ ] **B-BE.1.13** Implement public `GET /health` according to API Contract semantics.
- [ ] **B-BE.1.14** Add unit/integration smoke tests for config, response, health, timeout,
  cancellation, and shutdown.

Implement the combined API/PostgreSQL/Redis health contract in OpenAPI 0.4.0. A future
separate liveness endpoint requires an explicit contract/deployment revision.

## Milestone B-FE.1 — Frontend skeleton

- [ ] **B-FE.1.1** Initialize approved React, Tailwind, language, bundler, router, and package
  manager.
- [ ] **B-FE.1.2** Create feature-oriented structure from `hris-frontend/references/architecture.md`.
- [ ] **B-FE.1.3** Create validated public configuration and centralized API base boundary.
- [ ] **B-FE.1.4** Create global styles/design tokens and display the name **GSNpeeps**.
- [ ] **B-FE.1.5** Create AppShell, Sidebar, Topbar, PageContainer/PageHeader, skip link, and
  main landmark with synthetic placeholder content only.
- [ ] **B-FE.1.6** Create shared loading, empty, error, forbidden, and not-found components.
- [ ] **B-FE.1.7** Create provider composition for router, auth bootstrap boundary,
  server-state layer, errors, and feedback using approved tools.
- [ ] **B-FE.1.8** Create API error/envelope boundary aligned with OpenAPI.
- [ ] **B-FE.1.9** Configure API mocking boundary and deterministic synthetic fixture policy;
  keep mocks out of production.
- [ ] **B-FE.1.10** Configure approved format, lint, unit/component, E2E, and accessibility
  tools.
- [ ] **B-FE.1.11** Ensure static production output works through Nginx, including direct
  route fallback.
- [ ] **B-FE.1.12** Add smoke tests for app mount, shell semantics, responsive behavior,
  configuration error, and production mock exclusion.

Do not build business pages with invented data. Use only neutral skeleton/feedback examples
until feature contracts are consumed.

## File targets

Expected areas:

```text
docker-compose.yml
CLAUDE.md
docs/openapi.yaml
backend/
  cmd/api/
  cmd/worker/
  internal/config/
  internal/router/
  internal/middleware/
  internal/pkg/response/
  internal/platform/{postgres,redis,nextcloud,password,jwt,logger}/
  migrations/
  .env.example
  Dockerfile
frontend/
  src/app/
  src/routes/
  src/modules/
  src/components/{ui,layout,feedback}/
  src/lib/api/
  src/mocks/
  src/styles/
  src/test/
  .env.example
  Dockerfile
  package.json
  pnpm-lock.yaml
  vite.config.js
nginx/ or approved root Nginx config
```

Adapt to approved architecture without creating duplicate folder trees.

## Verification

Run repository-defined equivalents:

### Contract

- OpenAPI parse/lint with zero errors.
- Operation count and path/method comparison against API Contract v1.1.

### Backend

- Format, vet, lint, unit, integration, and build.
- API/worker start and graceful shutdown.
- Health success and dependency-failure behavior.
- Migration create/up/down-one/re-up on disposable database.

### Frontend

- Format, lint, unit/component tests, static production build.
- Direct-route Nginx fallback.
- Keyboard/landmark/skip-link/mobile smoke checks.
- Production bundle does not contain mocks/secrets.

### Infrastructure

- Compose config validation.
- All internal services reachable only as intended.
- Nginx is the only public ingress.
- Browser network/config inspection exposes no Nextcloud/database/Redis credentials.

Record exact commands and results; do not invent tool names before selection.

## Acceptance criteria

- [ ] Approved architecture/tooling choices are recorded.
- [ ] All service skeletons build reproducibly.
- [ ] API and worker run as separate processes.
- [ ] Docker topology matches System Design and only Nginx is public.
- [ ] `GET /health` behaves according to the approved contract.
- [ ] OpenAPI covers all 46 operations and validates without errors.
- [ ] Frontend produces static assets and renders accessible GSNpeeps shell/feedback states.
- [ ] Configuration fails clearly when required values are absent.
- [ ] No real secret, PII, production URL, or mock data enters production output.
- [ ] Required quality gates pass and are evidenced.

## Exit gate

Do not start Authentication/RBAC until:

- OpenAPI authentication and error contract is stable.
- Role identifiers are confirmed.
- PostgreSQL/Redis migration and connection foundation works.
- Token-storage/frontend bootstrap decision is documented without inventing refresh.
- Backend and frontend skeleton checks pass.
